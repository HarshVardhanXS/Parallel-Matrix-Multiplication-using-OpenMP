# Parallel-Matrix-Multiplication-using-OpenMP
Matrix multiplication is a core operation in scientific computing. This project demonstrates how OpenMP can significantly reduce execution time by parallelizing the computation across multiple CPU cores.
